filebeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false

# Logstash 서버의 주소를 입력합니다.
# Docker 네트워크를 사용하는 경우, Logstash 컨테이너의 서비스 이름을 사용합니다.
# 예: output.logstash: hosts: ["logstash:5044"]
# 다른 머신에 있는 경우, 해당 머신의 IP 주소를 입력합니다.
output.logstash:
  hosts:
